name := "scala-crud-api"

version := "0.1"

scalaVersion := "3.0.2"

lazy val root = (project in file("."))
  .settings(
    libraryDependencies ++= Seq(
      // Add your dependencies here if required
    )
  )
